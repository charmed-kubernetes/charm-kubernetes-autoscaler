bundle: kubernetes
description: Bundle to deploy for integration tests
applications:
  kubernetes-autoscaler:
    charm: {{charm}}
    options: {{juju_args}}
    trust: true
    scale: 1
    resources:
      juju-autoscaler-image: {{juju_autoscaler_image}}
relations: []
