bundle: kubernetes
description: Bundle to deploy for integration tests
applications:
  kubernetes-autoscaler:
    charm: {{charm}}
    options: {{juju_args}}
    trust: true
    scale: 1
    resources:
      juju-autoscaler-image: rocks.canonical.com/cdk/cluster-autoscaler-juju:dev-2.9.22-build4
  scale-app:
    charm: hello-kubecon
    scale: 1
relations: []
